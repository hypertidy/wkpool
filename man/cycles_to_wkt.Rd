% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/round_trip.R
\name{cycles_to_wkt}
\alias{cycles_to_wkt}
\alias{cycles_to_wkb}
\title{Convert cycles to WKT polygons}
\usage{
cycles_to_wkt(x, feature = TRUE, convention = c("sf", "ogc"))

cycles_to_wkb(x, feature = TRUE, convention = c("sf", "ogc"), ...)
}
\arguments{
\item{x}{A wkpool (ideally after merge_coincident)}

\item{feature}{Logical: attempt to reconstruct original features?
If TRUE, groups cycles by .feature and nests holes in outers.
If FALSE, each cycle becomes a separate polygon.}

\item{convention}{Winding convention: "sf" (default) or "ogc"; only
consulted for pools without path provenance}

\item{...}{Passed to \code{\link[wk:wkb]{wk::as_wkb()}}}
}
\value{
A wk_wkt vector of POLYGON geometries
}
\description{
Convert cycles to WKT polygons
}
\details{
Converts cycles back to polygons. When feature = TRUE and the pool
carries path provenance (minted by \code{\link[=establish_topology]{establish_topology()}}), the
original feature structure is reconstructed exactly: rings are
grouped into their original part (exterior first, then that part's
holes) and parts into their original feature (POLYGON or
MULTIPOLYGON), with winding emitted as stored and \code{convention} not
consulted. Pools without provenance fall back to a heuristic that
groups rings by feature and nests all holes with the first outer.

Geometry is assembled natively via \code{\link[wk:wk_linestring]{wk::wk_polygon()}} and
\code{\link[wk:wk_linestring]{wk::wk_collection()}} (WKB first, WKT derived from it): coordinates
are emitted at full double precision, rings are closed by wk, z is
carried when the pool has it, and the pool's crs and geodesic flag
are attached to the output.
}
\examples{
x <- wk::as_wkb(c(
  "POLYGON ((0 0, 1 0, 1 1, 0 1, 0 0))",
  "POLYGON ((1 0, 2 0, 2 1, 1 1, 1 0))"
))
pool <- establish_topology(x)
merged <- merge_coincident(pool)
cycles_to_wkt(merged)

}
