% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wk-handle.R
\name{wk_handle.wkpool}
\alias{wk_handle.wkpool}
\title{Handle wkpool objects with wk handlers}
\usage{
\method{wk_handle}{wkpool}(handleable, handler, ...)
}
\arguments{
\item{handleable}{A wkpool object.}

\item{handler}{A \link[wk:wk_handle]{wk handler}.}

\item{...}{Passed on.}
}
\value{
The result of the handler.
}
\description{
A wkpool is wk-handleable: it presents as one LINESTRING per segment
(so the handled vector has the same length as the pool vector), with
the pool's crs and geodesic flag attached. This means the wk
ecosystem's generic conversions work directly on a pool:
\code{wk::as_wkb()}, \code{wk::as_wkt()}, \code{wk::wk_coords()},
\code{geos::as_geos_geometry()}, plotting, and anything else built on
\code{\link[wk:wk_handle]{wk::wk_handle()}}.
}
\details{
Vertices are emitted at full double precision, indexed directly from
the pool (no text round trip). If the pool carries \code{z}, coordinates
are emitted as XYZ.

Arcs and cycles remain explicit conversions (\code{\link[=arcs_to_wkt]{arcs_to_wkt()}},
\code{\link[=cycles_to_wkt]{cycles_to_wkt()}} and friends): the handled form is deliberately the
segment vector itself.
}
\examples{
x <- wk::wkt(
  "POLYGON ((0 0, 1 0, 1 1, 0 1, 0 0))",
  crs = "EPSG:4326"
)
pool <- establish_topology(x)
wk::as_wkt(pool)
wk::wk_coords(pool)
wk::wk_crs(wk::as_wkb(pool))

}
