% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wkpool.R
\name{wkpool-crs}
\alias{wkpool-crs}
\alias{wk_crs.wkpool}
\alias{wk_set_crs.wkpool}
\alias{wk_is_geodesic.wkpool}
\alias{wk_set_geodesic.wkpool}
\title{CRS and geodesic handling for wkpool}
\usage{
\method{wk_crs}{wkpool}(x)

\method{wk_set_crs}{wkpool}(x, crs)

\method{wk_is_geodesic}{wkpool}(x)

\method{wk_set_geodesic}{wkpool}(x, geodesic)
}
\arguments{
\item{x}{A wkpool object.}

\item{crs}{A CRS object (commonly an authority string such as
"EPSG:4326"), or \code{NULL}.}

\item{geodesic}{\code{TRUE}, \code{FALSE}, or \code{NA} (inherit).}
}
\value{
\itemize{
\item \code{wk_crs()}: the CRS object, or \code{NULL}.
\item \code{wk_set_crs()}, \code{wk_set_geodesic()}: \code{x} with the attribute replaced.
\item \code{wk_is_geodesic()}: \code{TRUE}, \code{FALSE}, or \code{NA}.
}
}
\description{
A wkpool participates in wk's CRS propagation model: the CRS is an
opaque object carried in an attribute, never interpreted, and checked
for equality when pools are combined. Likewise the geodesic flag
records whether segments should be interpreted as geodesics when
coordinates are spherical. Both are captured from the input by
\code{\link[=establish_topology]{establish_topology()}}, survive subsetting, merging and compaction,
and are restored onto geometry produced by \code{\link[=segments_to_wkt]{segments_to_wkt()}},
\code{\link[=arcs_to_wkt]{arcs_to_wkt()}}, \code{\link[=cycles_to_wkt]{cycles_to_wkt()}} and their WKB counterparts.
}
\examples{
x <- wk::wkt("LINESTRING (0 0, 1 1)", crs = "EPSG:4326")
pool <- establish_topology(x)
wk::wk_crs(pool)
wk::wk_crs(wk::wk_set_crs(pool, "EPSG:3031"))

}
