% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wkpool.R
\name{new_wkpool}
\alias{new_wkpool}
\title{Construct a wkpool from validated components}
\usage{
new_wkpool(vertices, vx0, vx1, feature = NULL, crs = NULL, geodesic = FALSE)
}
\arguments{
\item{vertices}{A data.frame with columns \code{.vx} (integer vertex ids),
\code{x}, \code{y}, and optionally \code{z}.}

\item{vx0, vx1}{Integer vectors of segment start/end vertex ids, each
value present in \code{vertices$.vx}.}

\item{feature}{Optional integer vector of feature ids, one per segment.}

\item{crs}{A CRS object (commonly an authority string such as
"EPSG:4326"), carried but never interpreted, per wk's CRS
propagation model. Use \code{NULL} for none, or \code{\link[wk:wk_crs_inherit]{wk::wk_crs_inherit()}}.}

\item{geodesic}{\code{TRUE} if segments should be interpreted as geodesics
when coordinates are spherical, \code{FALSE} otherwise, \code{NA} to inherit.}
}
\value{
A wkpool object.
}
\description{
A low-level constructor for wkpool objects, for use by packages that
build pools directly (for example after refining or transforming an
existing pool). Inputs are checked against the wkpool invariants:
every segment endpoint must be a vertex id present in the pool.
}
\examples{
v <- data.frame(.vx = 1:3, x = c(0, 1, 1), y = c(0, 0, 1))
new_wkpool(v, vx0 = c(1L, 2L), vx1 = c(2L, 3L), crs = "EPSG:4326")

}
